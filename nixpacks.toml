[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python -m venv /app/.venv && /app/.venv/bin/pip install uv && /app/.venv/bin/uv sync --frozen --no-dev"]

[start]
cmd = "/app/.venv/bin/uvicorn src.app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
